namespace = linear_trees
country_event = {
  id = linear_trees.1319
  hide_window = yes
  is_triggered_only = yes
  trigger = { is_ai = no has_technology = "tech_violet_shield" }
  immediate = {
    if = {
      limit = {
        NOT = { has_tech_option = "tech_green_shield" }
      }
      add_research_option = "tech_green_shield"
    }
  }
}
country_event = {
  id = linear_trees.1320
  hide_window = yes
  is_triggered_only = yes
  trigger = { is_ai = no has_technology = "tech_green_shield" "tech_fusion_power_energon" }
  immediate = {
    if = {
      limit = {
        NOT = { has_tech_option = "tech_yellow_shield" }
      }
      add_research_option = "tech_yellow_shield"
    }
  }
}
country_event = {
  id = linear_trees.1321
  hide_window = yes
  is_triggered_only = yes
  trigger = { is_ai = no has_technology = "tech_yellow_shield" }
  immediate = {
    if = {
      limit = {
        NOT = { has_tech_option = "tech_orange_shield" }
      }
      add_research_option = "tech_orange_shield"
    }
  }
}
country_event = {
  id = linear_trees.1322
  hide_window = yes
  is_triggered_only = yes
  trigger = { is_ai = no has_technology = "tech_orange_shield" }
  immediate = {
    if = {
      limit = {
        NOT = { has_tech_option = "tech_red_shield" }
      }
      add_research_option = "tech_red_shield"
    }
  }
}
