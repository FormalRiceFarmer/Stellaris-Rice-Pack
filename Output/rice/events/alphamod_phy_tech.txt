namespace = linear_trees
country_event = {
  id = linear_trees.322
  hide_window = yes
  is_triggered_only = yes
  trigger = { is_ai = no has_technology = "tech_shields_1" }
  immediate = {
    if = {
      limit = {
        NOT = { has_tech_option = "tech_alpha_station_shields_1" }
      }
      add_research_option = "tech_alpha_station_shields_1"
    }
  }
}
country_event = {
  id = linear_trees.323
  hide_window = yes
  is_triggered_only = yes
  trigger = { is_ai = no has_technology = "tech_shields_2" "tech_alpha_station_shields_1" }
  immediate = {
    if = {
      limit = {
        NOT = { has_tech_option = "tech_alpha_station_shields_2" }
      }
      add_research_option = "tech_alpha_station_shields_2"
    }
  }
}
country_event = {
  id = linear_trees.324
  hide_window = yes
  is_triggered_only = yes
  trigger = { is_ai = no has_technology = "tech_shields_3" "tech_alpha_station_shields_2" }
  immediate = {
    if = {
      limit = {
        NOT = { has_tech_option = "tech_alpha_station_shields_3" }
      }
      add_research_option = "tech_alpha_station_shields_3"
    }
  }
}
country_event = {
  id = linear_trees.325
  hide_window = yes
  is_triggered_only = yes
  trigger = { is_ai = no has_technology = "tech_sensors_2" "tech_alpha_surveillance_stations" }
  immediate = {
    if = {
      limit = {
        NOT = { has_tech_option = "# tech_alpha_navigational_aids" }
      }
      add_research_option = "# tech_alpha_navigational_aids"
    }
  }
}
country_event = {
  id = linear_trees.326
  hide_window = yes
  is_triggered_only = yes
  trigger = { is_ai = no has_technology = "tech_alpha_galactic_comms" }
  immediate = {
    if = {
      limit = {
        NOT = { has_tech_option = "tech_alpha_gravitic_surveillance" }
      }
      add_research_option = "tech_alpha_gravitic_surveillance"
    }
  }
}
country_event = {
  id = linear_trees.327
  hide_window = yes
  is_triggered_only = yes
  trigger = { is_ai = no has_technology = "tech_sensors_3" "tech_alpha_gravitic_surveillance" }
  immediate = {
    if = {
      limit = {
        NOT = { has_tech_option = "tech_alpha_subspace_surveillance" }
      }
      add_research_option = "tech_alpha_subspace_surveillance"
    }
  }
}
country_event = {
  id = linear_trees.328
  hide_window = yes
  is_triggered_only = yes
  trigger = { is_ai = no has_technology = "tech_sensors_4" "tech_alpha_subspace_surveillance" }
  immediate = {
    if = {
      limit = {
        NOT = { has_tech_option = "tech_alpha_tachyon_surveillance" }
      }
      add_research_option = "tech_alpha_tachyon_surveillance"
    }
  }
}
country_event = {
  id = linear_trees.329
  hide_window = yes
  is_triggered_only = yes
  trigger = { is_ai = no has_technology = "tech_physics_lab_2" "tech_alpha_micro_fission" }
  immediate = {
    if = {
      limit = {
        NOT = { has_tech_option = "tech_alpha_research_capitals" }
      }
      add_research_option = "tech_alpha_research_capitals"
    }
  }
}
country_event = {
  id = linear_trees.330
  hide_window = yes
  is_triggered_only = yes
  trigger = { is_ai = no has_technology = "tech_alpha_galactic_comms" }
  immediate = {
    if = {
      limit = {
        NOT = { has_tech_option = "tech_alpha_centralised_comms" }
      }
      add_research_option = "tech_alpha_centralised_comms"
    }
  }
}
country_event = {
  id = linear_trees.331
  hide_window = yes
  is_triggered_only = yes
  trigger = { is_ai = no has_technology = "tech_sensors_2" }
  immediate = {
    if = {
      limit = {
        NOT = { has_tech_option = "tech_alpha_galactic_comms" }
      }
      add_research_option = "tech_alpha_galactic_comms"
    }
  }
}
country_event = {
  id = linear_trees.332
  hide_window = yes
  is_triggered_only = yes
  trigger = { is_ai = no has_technology = "tech_sensors_3" "tech_alpha_micro_fission" }
  immediate = {
    if = {
      limit = {
        NOT = { has_tech_option = "tech_alpha_subspace_comms" }
      }
      add_research_option = "tech_alpha_subspace_comms"
    }
  }
}
country_event = {
  id = linear_trees.333
  hide_window = yes
  is_triggered_only = yes
  trigger = { is_ai = no has_technology = "tech_fusion_power" }
  immediate = {
    if = {
      limit = {
        NOT = { has_tech_option = "tech_alpha_fusion_plants" }
      }
      add_research_option = "tech_alpha_fusion_plants"
    }
  }
}
country_event = {
  id = linear_trees.334
  hide_window = yes
  is_triggered_only = yes
  trigger = { is_ai = no has_technology = "tech_cold_fusion_power" "tech_alpha_fusion_plants" }
  immediate = {
    if = {
      limit = {
        NOT = { has_tech_option = "tech_alpha_cold_fusion" }
      }
      add_research_option = "tech_alpha_cold_fusion"
    }
  }
}
country_event = {
  id = linear_trees.335
  hide_window = yes
  is_triggered_only = yes
  trigger = { is_ai = no has_technology = "tech_alpha_cold_fusion" "tech_power_hub_1" }
  immediate = {
    if = {
      limit = {
        NOT = { has_tech_option = "" }
      }
      add_research_option = ""
    }
  }
}
country_event = {
  id = linear_trees.336
  hide_window = yes
  is_triggered_only = yes
  trigger = { is_ai = no has_technology = "tech_self_aware_logic" "tech_matter_replication_alpha" "tech_trade_ports" }
  immediate = {
    if = {
      limit = {
        NOT = { has_tech_option = "tech_alpha_commerce_capitals" }
      }
      add_research_option = "tech_alpha_commerce_capitals"
    }
  }
}
country_event = {
  id = linear_trees.337
  hide_window = yes
  is_triggered_only = yes
  trigger = { is_ai = no has_technology = "tech_merchant_companies" "tech_alpha_commerce_capitals" }
  immediate = {
    if = {
      limit = {
        NOT = { has_tech_option = "tech_power_banking" }
      }
      add_research_option = "tech_power_banking"
    }
  }
}
country_event = {
  id = linear_trees.338
  hide_window = yes
  is_triggered_only = yes
  trigger = { is_ai = no has_technology = "tech_fusion_power" }
  immediate = {
    if = {
      limit = {
        NOT = { has_tech_option = "tech_alpha_hydro_cells" }
      }
      add_research_option = "tech_alpha_hydro_cells"
    }
  }
}
country_event = {
  id = linear_trees.339
  hide_window = yes
  is_triggered_only = yes
  trigger = { is_ai = no has_technology = "tech_atmospheric_manipulation" "tech_matter_replication_alpha" }
  immediate = {
    if = {
      limit = {
        NOT = { has_tech_option = "tech_alpha_genesis_device" }
      }
      add_research_option = "tech_alpha_genesis_device"
    }
  }
}
