namespace = linear_trees
country_event = {
  id = linear_trees.439
  hide_window = yes
  is_triggered_only = yes
  trigger = { is_ai = no has_technology = "tech_spaceport_4" }
  immediate = {
    if = {
      limit = {
        NOT = { has_tech_option = "	icon" }
      }
      add_research_option = "	icon"
    }
  }
}
country_event = {
  id = linear_trees.440
  hide_window = yes
  is_triggered_only = yes
  trigger = { is_ai = no has_technology = "tech_spaceport_6" }
  immediate = {
    if = {
      limit = {
        NOT = { has_tech_option = "	icon" }
      }
      add_research_option = "	icon"
    }
  }
}
country_event = {
  id = linear_trees.441
  hide_window = yes
  is_triggered_only = yes
  trigger = { is_ai = no has_technology = "tech_spaceport_6" }
  immediate = {
    if = {
      limit = {
        NOT = { has_tech_option = "	icon" }
      }
      add_research_option = "	icon"
    }
  }
}
country_event = {
  id = linear_trees.442
  hide_window = yes
  is_triggered_only = yes
  trigger = { is_ai = no has_technology = "tech_spaceport_3" }
  immediate = {
    if = {
      limit = {
        NOT = { has_tech_option = "	icon" }
      }
      add_research_option = "	icon"
    }
  }
}
