namespace = linear_trees
country_event = {
  id = linear_trees.419
  hide_window = yes
  is_triggered_only = yes
  trigger = { is_ai = no has_technology = "tech_engineering_lab_1" }
  immediate = {
    if = {
      limit = {
        NOT = { has_tech_option = "	tier" }
      }
      add_research_option = "	tier"
    }
  }
}
country_event = {
  id = linear_trees.420
  hide_window = yes
  is_triggered_only = yes
  trigger = { is_ai = no has_technology = "tech_droid_workers" "tech_sentient_ai" }
  immediate = {
    if = {
      limit = {
        NOT = { has_tech_option = "	icon" }
      }
      add_research_option = "	icon"
    }
  }
}
country_event = {
  id = linear_trees.421
  hide_window = yes
  is_triggered_only = yes
  trigger = { is_ai = no has_technology = "tech_ship_armor_2" }
  immediate = {
    if = {
      limit = {
        NOT = { has_tech_option = "	icon" }
      }
      add_research_option = "	icon"
    }
  }
}
country_event = {
  id = linear_trees.422
  hide_window = yes
  is_triggered_only = yes
  trigger = { is_ai = no has_technology = "tech_strike_craft_1" }
  immediate = {
    if = {
      limit = {
        NOT = { has_tech_option = "	icon" }
      }
      add_research_option = "	icon"
    }
  }
}
