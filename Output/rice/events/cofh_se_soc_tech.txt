namespace = linear_trees
country_event = {
  id = linear_trees.431
  hide_window = yes
  is_triggered_only = yes
  trigger = { is_ai = no has_technology = tech_centralized_command }
  immediate = {
    if = {
      limit = {
        NOT = { has_tech_option = "	icon" }
      }
      add_research_option = "	icon"
    }
  }
}
country_event = {
  id = linear_trees.432
  hide_window = yes
  is_triggered_only = yes
  trigger = { is_ai = no has_technology = tech_doctrine_fleet_size_3 }
  immediate = {
    if = {
      limit = {
        NOT = { has_tech_option = "	icon" }
      }
      add_research_option = "	icon"
    }
  }
}
country_event = {
  id = linear_trees.433
  hide_window = yes
  is_triggered_only = yes
  trigger = { is_ai = no has_technology = tech_galactic_administration }
  immediate = {
    if = {
      limit = {
        NOT = { has_tech_option = "	icon" }
      }
      add_research_option = "	icon"
    }
  }
}
country_event = {
  id = linear_trees.434
  hide_window = yes
  is_triggered_only = yes
  trigger = { is_ai = no has_technology = "tech_psionic_theory" }
  immediate = {
    if = {
      limit = {
        NOT = { has_tech_option = "	icon" }
      }
      add_research_option = "	icon"
    }
  }
}
country_event = {
  id = linear_trees.435
  hide_window = yes
  is_triggered_only = yes
  trigger = { is_ai = no has_technology = "tech_galactic_ambitions" }
  immediate = {
    if = {
      limit = {
        NOT = { has_tech_option = "	icon" }
      }
      add_research_option = "	icon"
    }
  }
}
country_event = {
  id = linear_trees.436
  hide_window = yes
  is_triggered_only = yes
  trigger = { is_ai = no has_technology = "tech_galactic_ambitions" }
  immediate = {
    if = {
      limit = {
        NOT = { has_tech_option = "	icon" }
      }
      add_research_option = "	icon"
    }
  }
}
