namespace = linear_trees
country_event = {
  id = linear_trees.620
  hide_window = yes
  is_triggered_only = yes
  trigger = { is_ai = no has_technology = "tech_newtype_theory" }
  immediate = {
    if = {
      limit = {
        NOT = { has_tech_option = "tech_psycommu" }
      }
      add_research_option = "tech_psycommu"
    }
  }
}
country_event = {
  id = linear_trees.621
  hide_window = yes
  is_triggered_only = yes
  trigger = { is_ai = no has_technology = "tech_newtype_theory" }
  immediate = {
    if = {
      limit = {
        NOT = { has_tech_option = "tech_cyber_newtypes" }
      }
      add_research_option = "tech_cyber_newtypes"
    }
  }
}
country_event = {
  id = linear_trees.622
  hide_window = yes
  is_triggered_only = yes
  trigger = { is_ai = no has_technology = "tech_EFSF_BEAMC_1" }
  immediate = {
    if = {
      limit = {
        NOT = { has_tech_option = "tech_efsf_newtype_theory" }
      }
      add_research_option = "tech_efsf_newtype_theory"
    }
  }
}
country_event = {
  id = linear_trees.623
  hide_window = yes
  is_triggered_only = yes
  trigger = { is_ai = no has_technology = "tech_efsf_newtype_theory" }
  immediate = {
    if = {
      limit = {
        NOT = { has_tech_option = "tech_efsf_cyber_newtypes" }
      }
      add_research_option = "tech_efsf_cyber_newtypes"
    }
  }
}
