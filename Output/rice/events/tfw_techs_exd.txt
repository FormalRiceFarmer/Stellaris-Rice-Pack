namespace = linear_trees
country_event = {
  id = linear_trees.1288
  hide_window = yes
  is_triggered_only = yes
  trigger = { is_ai = no has_technology = tech_tfw_exd_altion }
  immediate = {
    if = {
      limit = {
        NOT = { has_tech_option = "tech_tfw_extradimensional_weapon_2" }
      }
      add_research_option = "tech_tfw_extradimensional_weapon_2"
    }
  }
}
