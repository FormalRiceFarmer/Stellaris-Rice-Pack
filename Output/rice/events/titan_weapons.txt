namespace = linear_trees
country_event = {
  id = linear_trees.1391
  hide_window = yes
  is_triggered_only = yes
  trigger = { is_ai = no has_technology = "tech_extradimensional_weapon_1" "tech_titan_weaponry" }
  immediate = {
    if = {
      limit = {
        NOT = { has_tech_option = "" }
      }
      add_research_option = ""
    }
  }
}
country_event = {
  id = linear_trees.1392
  hide_window = yes
  is_triggered_only = yes
  trigger = { is_ai = no has_technology = "tech_advanced_arc_emitter" "tech_titan_weaponry" }
  immediate = {
    if = {
      limit = {
        NOT = { has_tech_option = "" }
      }
      add_research_option = ""
    }
  }
}
country_event = {
  id = linear_trees.1393
  hide_window = yes
  is_triggered_only = yes
  trigger = { is_ai = no has_technology = "tech_titan_arc_emitter_prototype" }
  immediate = {
    if = {
      limit = {
        NOT = { has_tech_option = "" }
      }
      add_research_option = ""
    }
  }
}
country_event = {
  id = linear_trees.1394
  hide_window = yes
  is_triggered_only = yes
  trigger = { is_ai = no has_technology = "tech_dragon_armor" "tech_titan_weaponry" }
  immediate = {
    if = {
      limit = {
        NOT = { has_tech_option = "tech_dragon_breath" }
      }
      add_research_option = "tech_dragon_breath"
    }
  }
}
country_event = {
  id = linear_trees.1395
  hide_window = yes
  is_triggered_only = yes
  trigger = { is_ai = no has_technology = "tech_electromagnetic_beam" "tech_titan_weaponry" }
  immediate = {
    if = {
      limit = {
        NOT = { has_tech_option = "tech_solar_disruptors" }
      }
      add_research_option = "tech_solar_disruptors"
    }
  }
}
country_event = {
  id = linear_trees.1396
  hide_window = yes
  is_triggered_only = yes
  trigger = { is_ai = no has_technology = "tech_advanced_kinetic_weapon" "tech_titan_weaponry" }
  immediate = {
    if = {
      limit = {
        NOT = { has_tech_option = "tech_terror_cannon_prototype" }
      }
      add_research_option = "tech_terror_cannon_prototype"
    }
  }
}
country_event = {
  id = linear_trees.1397
  hide_window = yes
  is_triggered_only = yes
  trigger = { is_ai = no has_technology = "tech_terror_cannon_prototype" }
  immediate = {
    if = {
      limit = {
        NOT = { has_tech_option = "tech_terror_cannon" }
      }
      add_research_option = "tech_terror_cannon"
    }
  }
}
country_event = {
  id = linear_trees.1398
  hide_window = yes
  is_triggered_only = yes
  trigger = { is_ai = no has_technology = "tech_smart_torpedoes" "tech_titan_weaponry" }
  immediate = {
    if = {
      limit = {
        NOT = { has_tech_option = "tech_titan_torpedoes" }
      }
      add_research_option = "tech_titan_torpedoes"
    }
  }
}
country_event = {
  id = linear_trees.1399
  hide_window = yes
  is_triggered_only = yes
  trigger = { is_ai = no has_technology = "tech_scourge_missile_1" "tech_titan_weaponry" }
  immediate = {
    if = {
      limit = {
        NOT = { has_tech_option = "tech_scourge_artillery" }
      }
      add_research_option = "tech_scourge_artillery"
    }
  }
}
country_event = {
  id = linear_trees.1400
  hide_window = yes
  is_triggered_only = yes
  trigger = { is_ai = no has_technology = "tech_hellfire_autocannons" "tech_titan_weaponry" }
  immediate = {
    if = {
      limit = {
        NOT = { has_tech_option = "tech_massive_autocannons" }
      }
      add_research_option = "tech_massive_autocannons"
    }
  }
}
country_event = {
  id = linear_trees.1401
  hide_window = yes
  is_triggered_only = yes
  trigger = { is_ai = no has_technology = "tech_integrated_missile_system" "tech_titan_weaponry" }
  immediate = {
    if = {
      limit = {
        NOT = { has_tech_option = "tech_titan_missile" }
      }
      add_research_option = "tech_titan_missile"
    }
  }
}
country_event = {
  id = linear_trees.1402
  hide_window = yes
  is_triggered_only = yes
  trigger = { is_ai = no has_technology = "tech_plasma_launcher" "tech_titan_weaponry" }
  immediate = {
    if = {
      limit = {
        NOT = { has_tech_option = "tech_advanced_plasma_launcher" }
      }
      add_research_option = "tech_advanced_plasma_launcher"
    }
  }
}
country_event = {
  id = linear_trees.1403
  hide_window = yes
  is_triggered_only = yes
  trigger = { is_ai = no has_technology = "tech_fallen_technologies_1" "tech_titan_weaponry" }
  immediate = {
    if = {
      limit = {
        NOT = { has_tech_option = "tech_mysterious_weapon" }
      }
      add_research_option = "tech_mysterious_weapon"
    }
  }
}
country_event = {
  id = linear_trees.1404
  hide_window = yes
  is_triggered_only = yes
  trigger = { is_ai = no has_technology = "tech_fallen_technologies_2" "tech_mysterious_weapon" }
  immediate = {
    if = {
      limit = {
        NOT = { has_tech_option = "tech_portal_generator" }
      }
      add_research_option = "tech_portal_generator"
    }
  }
}
