namespace = linear_trees
country_event = {
  id = linear_trees.516
  hide_window = yes
  is_triggered_only = yes
  trigger = { is_ai = no has_technology = "tech_c_shield_rechargers_2" }
  immediate = {
    if = {
      limit = {
        NOT = { has_tech_option = "tech_c_shield_rechargers_3" }
      }
      add_research_option = "tech_c_shield_rechargers_3"
    }
  }
}
