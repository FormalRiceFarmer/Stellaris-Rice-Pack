namespace = linear_trees
country_event = {
  id = linear_trees.1374
  hide_window = yes
  is_triggered_only = yes
  trigger = { is_ai = no has_technology = "tech_shields_5" }
  immediate = {
    if = {
      limit = {
        NOT = { has_tech_option = "" }
      }
      add_research_option = ""
    }
  }
}
country_event = {
  id = linear_trees.1375
  hide_window = yes
  is_triggered_only = yes
  trigger = { is_ai = no has_technology = "tech_titan_reactor_prototype" }
  immediate = {
    if = {
      limit = {
        NOT = { has_tech_option = "" }
      }
      add_research_option = ""
    }
  }
}
country_event = {
  id = linear_trees.1376
  hide_window = yes
  is_triggered_only = yes
  trigger = { is_ai = no has_technology = "tech_zero_point_power" }
  immediate = {
    if = {
      limit = {
        NOT = { has_tech_option = "" }
      }
      add_research_option = ""
    }
  }
}
country_event = {
  id = linear_trees.1377
  hide_window = yes
  is_triggered_only = yes
  trigger = { is_ai = no has_technology = "tech_titan_hull_prototype" }
  immediate = {
    if = {
      limit = {
        NOT = { has_tech_option = "" }
      }
      add_research_option = ""
    }
  }
}
country_event = {
  id = linear_trees.1378
  hide_window = yes
  is_triggered_only = yes
  trigger = { is_ai = no has_technology = "tech_ship_armor_5" }
  immediate = {
    if = {
      limit = {
        NOT = { has_tech_option = "" }
      }
      add_research_option = ""
    }
  }
}
country_event = {
  id = linear_trees.1379
  hide_window = yes
  is_triggered_only = yes
  trigger = { is_ai = no has_technology = "tech_titan_armor_prototype" }
  immediate = {
    if = {
      limit = {
        NOT = { has_tech_option = "" }
      }
      add_research_option = ""
    }
  }
}
country_event = {
  id = linear_trees.1380
  hide_window = yes
  is_triggered_only = yes
  trigger = { is_ai = no has_technology = "tech_ship_armor_5" }
  immediate = {
    if = {
      limit = {
        NOT = { has_tech_option = "" }
      }
      add_research_option = ""
    }
  }
}
country_event = {
  id = linear_trees.1381
  hide_window = yes
  is_triggered_only = yes
  trigger = { is_ai = no has_technology = "tech_titan_thruster_prototype" }
  immediate = {
    if = {
      limit = {
        NOT = { has_tech_option = "" }
      }
      add_research_option = ""
    }
  }
}
country_event = {
  id = linear_trees.1382
  hide_window = yes
  is_triggered_only = yes
  trigger = { is_ai = no has_technology = "tech_thrusters_4" }
  immediate = {
    if = {
      limit = {
        NOT = { has_tech_option = "" }
      }
      add_research_option = ""
    }
  }
}
country_event = {
  id = linear_trees.1383
  hide_window = yes
  is_triggered_only = yes
  trigger = { is_ai = no has_technology = "tech_titan_sensor_prototype" }
  immediate = {
    if = {
      limit = {
        NOT = { has_tech_option = "" }
      }
      add_research_option = ""
    }
  }
}
country_event = {
  id = linear_trees.1384
  hide_window = yes
  is_triggered_only = yes
  trigger = { is_ai = no has_technology = "tech_sensors_4" }
  immediate = {
    if = {
      limit = {
        NOT = { has_tech_option = "" }
      }
      add_research_option = ""
    }
  }
}
country_event = {
  id = linear_trees.1385
  hide_window = yes
  is_triggered_only = yes
  trigger = { is_ai = no has_technology = "tech_titan_jump_drive_prototype" }
  immediate = {
    if = {
      limit = {
        NOT = { has_tech_option = "" }
      }
      add_research_option = ""
    }
  }
}
country_event = {
  id = linear_trees.1386
  hide_window = yes
  is_triggered_only = yes
  trigger = { is_ai = no has_technology = "tech_jump_drive_1" }
  immediate = {
    if = {
      limit = {
        NOT = { has_tech_option = "" }
      }
      add_research_option = ""
    }
  }
}
country_event = {
  id = linear_trees.1387
  hide_window = yes
  is_triggered_only = yes
  trigger = { is_ai = no has_technology = "tech_titan_afterburner_prototype" }
  immediate = {
    if = {
      limit = {
        NOT = { has_tech_option = "" }
      }
      add_research_option = ""
    }
  }
}
country_event = {
  id = linear_trees.1388
  hide_window = yes
  is_triggered_only = yes
  trigger = { is_ai = no has_technology = "tech_afterburners_2" }
  immediate = {
    if = {
      limit = {
        NOT = { has_tech_option = "" }
      }
      add_research_option = ""
    }
  }
}
country_event = {
  id = linear_trees.1389
  hide_window = yes
  is_triggered_only = yes
  trigger = { is_ai = no has_technology = "tech_titan_weaponry" }
  immediate = {
    if = {
      limit = {
        NOT = { has_tech_option = "" }
      }
      add_research_option = ""
    }
  }
}
country_event = {
  id = linear_trees.1390
  hide_window = yes
  is_triggered_only = yes
  trigger = { is_ai = no has_technology = "tech_titan_weaponry" }
  immediate = {
    if = {
      limit = {
        NOT = { has_tech_option = "" }
      }
      add_research_option = ""
    }
  }
}
