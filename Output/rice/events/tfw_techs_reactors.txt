namespace = linear_trees
country_event = {
  id = linear_trees.1308
  hide_window = yes
  is_triggered_only = yes
  trigger = { is_ai = no has_technology = "tech_fusion_power_energon" }
  immediate = {
    if = {
      limit = {
        NOT = { has_tech_option = "tech_antimatter_power_optimized" }
      }
      add_research_option = "tech_antimatter_power_optimized"
    }
  }
}
country_event = {
  id = linear_trees.1309
  hide_window = yes
  is_triggered_only = yes
  trigger = { is_ai = no has_technology = "tech_antimatter_power_optimized" "tech_mine_dark_matter" }
  immediate = {
    if = {
      limit = {
        NOT = { has_tech_option = "tech_darkmatter_power" }
      }
      add_research_option = "tech_darkmatter_power"
    }
  }
}
country_event = {
  id = linear_trees.1310
  hide_window = yes
  is_triggered_only = yes
  trigger = { is_ai = no has_technology = "tech_darkmatter_power" }
  immediate = {
    if = {
      limit = {
        NOT = { has_tech_option = "tech_darkmatter_power_optimized" }
      }
      add_research_option = "tech_darkmatter_power_optimized"
    }
  }
}
country_event = {
  id = linear_trees.1311
  hide_window = yes
  is_triggered_only = yes
  trigger = { is_ai = no has_technology = "tech_darkmatter_power_optimized" }
  immediate = {
    if = {
      limit = {
        NOT = { has_tech_option = "tech_singularity_power" }
      }
      add_research_option = "tech_singularity_power"
    }
  }
}
country_event = {
  id = linear_trees.1312
  hide_window = yes
  is_triggered_only = yes
  trigger = { is_ai = no has_technology = "tech_singularity_power" }
  immediate = {
    if = {
      limit = {
        NOT = { has_tech_option = "tech_singularity_power_opt" }
      }
      add_research_option = "tech_singularity_power_opt"
    }
  }
}
country_event = {
  id = linear_trees.1313
  hide_window = yes
  is_triggered_only = yes
  trigger = { is_ai = no has_technology = "tech_darkmatter_power_optimized" "tech_tfw_rapid_cell_replication" }
  immediate = {
    if = {
      limit = {
        NOT = { has_tech_option = "tech_bio_power_1" }
      }
      add_research_option = "tech_bio_power_1"
    }
  }
}
country_event = {
  id = linear_trees.1314
  hide_window = yes
  is_triggered_only = yes
  trigger = { is_ai = no has_technology = "tech_bio_power_1" }
  immediate = {
    if = {
      limit = {
        NOT = { has_tech_option = "tech_bio_power_2" }
      }
      add_research_option = "tech_bio_power_2"
    }
  }
}
country_event = {
  id = linear_trees.1315
  hide_window = yes
  is_triggered_only = yes
  trigger = { is_ai = no has_technology = "tech_singularity_power" "tech_tfw_adv_weapons" }
  immediate = {
    if = {
      limit = {
        NOT = { has_tech_option = "tech_tachyon_power" }
      }
      add_research_option = "tech_tachyon_power"
    }
  }
}
country_event = {
  id = linear_trees.1316
  hide_window = yes
  is_triggered_only = yes
  trigger = { is_ai = no has_technology = "tech_tachyon_power" }
  immediate = {
    if = {
      limit = {
        NOT = { has_tech_option = "tech_wimp_power" }
      }
      add_research_option = "tech_wimp_power"
    }
  }
}
country_event = {
  id = linear_trees.1317
  hide_window = yes
  is_triggered_only = yes
  trigger = { is_ai = no has_technology = "tech_singularity_power" "tech_tfw_focused_psi_flows" }
  immediate = {
    if = {
      limit = {
        NOT = { has_tech_option = "tech_psi_power_1" }
      }
      add_research_option = "tech_psi_power_1"
    }
  }
}
country_event = {
  id = linear_trees.1318
  hide_window = yes
  is_triggered_only = yes
  trigger = { is_ai = no has_technology = "tech_psi_power_1" }
  immediate = {
    if = {
      limit = {
        NOT = { has_tech_option = "tech_psi_power_2" }
      }
      add_research_option = "tech_psi_power_2"
    }
  }
}
